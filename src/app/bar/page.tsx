import { StationBoard } from "@/components/station-board";

export default function BarPage() {
  return (
    <main className="flex flex-1">
      <StationBoard station="bar" title="Bar" />
    </main>
  );
}
